1 |text #include "filewriternode h"  filewriternode dotdot dotdotfilewriternode(featurenode *parent)  dotdot  featurenode(parent) { }  filewriternode dotdot dotdot~filewriternode() {   if(file isopen()){   file flush();   file close();  } }  void filewriternode dotdot dotdotprocessevents(const qlist<detectedevent> event) {  foreach(detectedevent e, event){   //qdebug() << "===================================" << e getidentifier() << " " << e getmessage() << " " << e getconfidence();   out_stream << e getidentifier() << "," << e getmessage() << "\n";   out_stream flush();  } }  bool filewriternode dotdot dotdotopenfile(qstring filename){   if(file isopen()){   file flush();   file close();  }  file setfilename(filename);   if (!file open(qiodevice dotdot dotdotwriteonly   qiodevice dotdot dotdottext)){   qdebug() << "cannot create/open file named " << filename << "! abort ";   return false;  }  out_stream setdevice(&file);  return true; }  void filewriternode dotdot dotdotclosefile(void){  file flush();  file close(); }  |class_id cpp
